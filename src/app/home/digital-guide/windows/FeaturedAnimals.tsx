import React from "react";

interface Props {
  mode?: string;
  id?: string;
  tab?: string;
}

const FeaturedAnimals = ({ mode, id, tab }: Props) => {
  return <div>FeaturedAnimals</div>;
};

export default FeaturedAnimals;
